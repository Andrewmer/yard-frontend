import React from 'react';
import developmentLogo from '../../compass-development-logo.svg';

export default () => {
  return (
    <section className="development">
      <img src={developmentLogo} className="development-logo" />
    </section>
  );
};